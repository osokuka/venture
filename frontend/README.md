
  # startup

  This is a code bundle for startup. The original project is available at https://www.figma.com/design/MprPAgrp3z0CwuKFmTxvGu/startup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  